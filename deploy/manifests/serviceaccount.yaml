---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: lb-operator-serviceaccount
  labels:
    app.kubernetes.io/name: lb-operator
    app.kubernetes.io/instance: lb-operator
    app.kubernetes.io/version: "0.1.0"
---
apiVersion: rbac.authorization.k8s.io/v1
# This cluster role binding allows anyone in the "manager" group to read secrets in any namespace.
kind: ClusterRoleBinding
metadata:
  name: lb-operator-clusterrolebinding
  labels:
    app.kubernetes.io/name: lb-operator
    app.kubernetes.io/instance: lb-operator
    app.kubernetes.io/version: "0.1.0"
subjects:
  - kind: ServiceAccount
    name: lb-operator-serviceaccount
    namespace: default
roleRef:
  kind: ClusterRole
  name: lb-operator-clusterrole
  apiGroup: rbac.authorization.k8s.io
