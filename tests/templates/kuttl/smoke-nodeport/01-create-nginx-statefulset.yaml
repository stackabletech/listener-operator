---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: nginx
spec:
  serviceName: nginx
  selector:
    matchLabels:
      app: nginx
  replicas: 2
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx:1.23.1
          ports:
            - name: http
              containerPort: 80
          volumeMounts:
            - name: lb
              mountPath: /lb
            - name: metadata
              mountPath: /usr/share/nginx/html/
      volumes:
        - name: metadata
          downwardAPI:
            items:
              - path: pod-name
                fieldRef:
                  fieldPath: metadata.name
  volumeClaimTemplates:
    - metadata:
        name: lb
        annotations:
          lb.stackable.tech/lb-class: lb-operator-test-smoke-nodeport
      spec:
        accessModes:
        - ReadWriteMany
        storageClassName: lb.stackable.tech
        resources:
          requests:
            storage: 1

