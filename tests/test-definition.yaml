---
dimensions:
  - name: openshift
    values:
      - "false"
  - name: addressType
    values:
      - IP
      - Hostname
      - HostnameConservative
tests:
  - name: smoke-nodeport
    dimensions:
      - openshift
      - addressType
  # Not a "regular" test dimension since custom lbclasses won't be possible to /connect/ to,
  # we just want to check that they get created correctly
  - name: custom-lbclass
    dimensions:
      - openshift
suites:
  - name: nightly
  - name: openshift
    patch:
      - dimensions:
          - name: openshift
            expr: "true"
